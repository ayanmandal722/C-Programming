#include<stdio.h>
int main(){
//to get the decimal value of the devision of intigers we use float type
float x = 5;
float y = 2;
float z = x / y;
printf("%f",z);
float w = 7 / 2;
printf("\n%f",w);
//if we don't write the integer value as a decimal it will not giving output in decimal.
//we have to store the integer value in decimal as well
float u = 9.0 / 2;
printf("\n%f",u);
return 0;
}